# Backend Service

## Description
Express.js API serving sales data.

## Scripts
- `npm start`: Runs the server (node src/index.js)
- `npm run dev`: Runs with nodemon

## API Endpoints
- `GET /api/sales`: Main data endpoint. Accepts query params.
- `GET /api/sales/filters`: Returns unique values for filter dropdowns.
